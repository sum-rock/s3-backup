# Fill these values in on a ".env" file in the project root to complete tests
amazon:
  aws_access_key_id:
  aws_secret_access_key:
  bucket_name: 
  hosted_region:
  folder_path: 

backups:
  restore_dir: "tests/home/restore"
  include_dirs:
    - "tests/home/source"
  exclude_dirs:
    - "tests/home/source/melville"

options:
  weeks_until_log_purge: 5
  weeks_until_full_backup: 4
  passphrase: "cyGficReKVrd.R4drrqvb*-Y"
